# TransitionIQ Environment Variables
# Copy this file to .env.local and fill in your values:
#   cp .env.example .env.local

# ============================================================
# REQUIRED — Observability
# ============================================================

# Opik (Comet) API key — required for tracing, prompt versioning, evaluation
# Get your key from: https://www.comet.com/opik
OPIK_API_KEY=your_opik_api_key_here
OPIK_PROJECT_NAME=transitioniq

# ============================================================
# REQUIRED — At least ONE LLM provider key
# ============================================================

# OpenAI — GPT-4o Mini + voice features (TTS & STT)
# Get your key from: https://platform.openai.com
# NOTE: This key also powers text-to-speech (tts-1) and speech-to-text (Whisper).
#       Without it, voice features will be disabled but the app still works for text.
OPENAI_API_KEY=your_openai_key_here

# HuggingFace — Qwen3 8B, Qwen3 30B (free tier available)
# Get your token from: https://huggingface.co/settings/tokens
HF_API_KEY=your_huggingface_key_here

# Google Gemini — 2.5 Flash, 2.5 Flash Lite
# Get your key from: https://aistudio.google.com
GEMINI_API_KEY=your_gemini_key_here

# ============================================================
# OPTIONAL — Configuration
# ============================================================

# Override default LLM model selection
# Options: openai-gpt-4o-mini, hf-qwen3-8b, hf-qwen3-30b-a3b, gemini-2.5-flash, gemini-2.5-flash-lite
# LLM_MODEL=openai-gpt-4o-mini

# Admin bypass for rate limits during live demos
# Set this, then in browser console: document.cookie = "tiq_admin=YOUR_SECRET; path=/; max-age=86400"
# ADMIN_SECRET=your_secret_here

# Enable the source code viewer at /source (for hackathon judges)
# CODE_VIEWER_ENABLED=true

# Hide the evaluation tab from the UI
# NEXT_PUBLIC_DISABLE_EVALUATION=true

# FHIR server URL (default: SMART Health IT Sandbox)
# FHIR_BASE_URL=https://launch.smarthealthit.org/v/r4/fhir

# ============================================================
# OPTIONAL — Voice rate limits (defaults shown)
# ============================================================

# Text-to-speech (OpenAI tts-1)
# VOICE_RATE_LIMIT_MAX=5
# VOICE_RATE_LIMIT_WINDOW_MIN=1440

# Speech-to-text (OpenAI Whisper — Firefox fallback)
# STT_RATE_LIMIT_MAX=10
# STT_RATE_LIMIT_WINDOW_MIN=60
